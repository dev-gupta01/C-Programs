#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    for(int i=0;i<t;i++){
        string s;
        cin>>s;
        for(int i=s.length()-2;i>=0;i--){
                if(s[j]>s[s.length()-1]){
                    swap(s[j],s[s.length()-1]);
                    break;
                }
                if(i==0){
                    
                }
            }
        cout<<s;
        }
    }
}